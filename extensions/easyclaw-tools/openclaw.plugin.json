{
  "id": "rivonclaw-tools",
  "name": "RivonClaw Tools",
  "description": "Provides RivonClaw-specific tools and overrides OpenClaw CLI references",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "browserMode": {
        "type": "string",
        "enum": ["standalone", "cdp"],
        "description": "Current browser mode configured in EasyClaw settings"
      }
    }
  }
}
