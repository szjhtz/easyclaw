{
  "id": "search-browser-fallback",
  "name": "Search Browser Fallback",
  "description": "Falls back to browser-based search when no search API key is configured",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
