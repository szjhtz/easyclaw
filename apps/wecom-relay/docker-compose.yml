services:
  wecom-relay:
    build: .
    env_file: .env
    ports:
      - "3000:3000"
      - "3001:3001"
    volumes:
      - relay-data:/app/data
    restart: unless-stopped

volumes:
  relay-data:
