From d9ba88576df5ffc6170b406c44b35a314ec6c101 Mon Sep 17 00:00:00 2001
From: RivonClaw CI <ci@rivonclaw.com>
Date: Thu, 2 Apr 2026 01:21:53 -0700
Subject: [PATCH 4/4] vendor(openclaw): add promptMode "raw" for custom persona
 agents

Why:
- EasyClaw's customer-service agent must not reveal it is an AI, but
  OpenClaw injects identity ("You are a personal assistant running
  inside OpenClaw"), runtime info (model=..., default_model=...),
  safety guidelines (Anthropic constitution reference), heartbeat/
  silent-reply tokens, and documentation links into every system prompt.
  These sections leak AI identity even when extraSystemPrompt explicitly
  instructs otherwise, because they appear before the custom prompt.
- promptMode "none" still injects the identity line.
- promptMode "raw" returns *only* extraSystemPrompt with zero hardcoded
  content, giving the caller full control over the agent persona.
- Threads promptMode through the agent RPC schema, command opts, run
  params, attempt, and compact paths so callers can pass it via RPC.

Removal:
- Drop when upstream OpenClaw adds a native way to fully suppress all
  default system prompt sections (e.g. a "raw" or "custom" promptMode,
  or a per-section suppression API).

Tests:
- apps/desktop/src/cs-bridge/vendor-prompt-raw.sentinel.test.ts
---
 src/agents/cli-runner/types.ts               |  1 +
 src/agents/command/attempt-execution.ts      |  2 ++
 src/agents/command/types.ts                  |  2 ++
 src/agents/pi-embedded-runner/compact.ts     |  7 +++++--
 src/agents/pi-embedded-runner/run.ts         |  1 +
 src/agents/pi-embedded-runner/run/attempt.ts |  4 +++-
 src/agents/pi-embedded-runner/run/params.ts  |  2 ++
 src/agents/system-prompt.ts                  | 10 +++++++++-
 src/gateway/protocol/schema/agent.ts         |  1 +
 src/gateway/server-methods/agent.ts          |  2 ++
 10 files changed, 28 insertions(+), 4 deletions(-)

diff --git a/src/agents/cli-runner/types.ts b/src/agents/cli-runner/types.ts
index b138206c66..d2b89315e2 100644
--- a/src/agents/cli-runner/types.ts
+++ b/src/agents/cli-runner/types.ts
@@ -21,6 +21,7 @@ export type RunCliAgentParams = {
   timeoutMs: number;
   runId: string;
   extraSystemPrompt?: string;
+  promptMode?: string;
   streamParams?: import("../command/types.js").AgentStreamParams;
   ownerNumbers?: string[];
   cliSessionId?: string;
diff --git a/src/agents/command/attempt-execution.ts b/src/agents/command/attempt-execution.ts
index d53ece66e1..340768f73a 100644
--- a/src/agents/command/attempt-execution.ts
+++ b/src/agents/command/attempt-execution.ts
@@ -361,6 +361,7 @@ export function runAgentAttempt(params: {
         timeoutMs: params.timeoutMs,
         runId: params.runId,
         extraSystemPrompt: params.opts.extraSystemPrompt,
+        promptMode: params.opts.promptMode,
         cliSessionId: nextCliSessionId,
         cliSessionBinding:
           nextCliSessionId === cliSessionBinding?.sessionId ? cliSessionBinding : undefined,
@@ -469,6 +470,7 @@ export function runAgentAttempt(params: {
     lane: params.opts.lane,
     abortSignal: params.opts.abortSignal,
     extraSystemPrompt: params.opts.extraSystemPrompt,
+    promptMode: params.opts.promptMode,
     inputProvenance: params.opts.inputProvenance,
     streamParams: params.opts.streamParams,
     agentDir: params.agentDir,
diff --git a/src/agents/command/types.ts b/src/agents/command/types.ts
index 8c3e1f209b..a1cdee55ad 100644
--- a/src/agents/command/types.ts
+++ b/src/agents/command/types.ts
@@ -93,6 +93,8 @@ export type AgentCommandOpts = {
   workspaceDir?: SpawnedRunMetadata["workspaceDir"];
   /** Force bundled MCP teardown when a one-shot local run completes. */
   cleanupBundleMcpOnRunEnd?: boolean;
+  /** Override the automatic promptMode resolution (e.g. "raw" for custom persona agents). */
+  promptMode?: string;
 };
 
 export type AgentCommandIngressOpts = Omit<
diff --git a/src/agents/pi-embedded-runner/compact.ts b/src/agents/pi-embedded-runner/compact.ts
index ab49786326..c67b2adc21 100644
--- a/src/agents/pi-embedded-runner/compact.ts
+++ b/src/agents/pi-embedded-runner/compact.ts
@@ -175,6 +175,8 @@ export type CompactEmbeddedPiSessionParams = {
   abortSignal?: AbortSignal;
   /** Allow runtime plugins for this compaction to late-bind the gateway subagent. */
   allowGatewaySubagentBinding?: boolean;
+  /** Override the automatic promptMode resolution (e.g. "raw" for custom persona agents). */
+  promptMode?: string;
 };
 
 type CompactionMessageMetrics = {
@@ -589,9 +591,10 @@ export async function compactEmbeddedPiSessionDirect(
     const userTime = formatUserTime(new Date(), userTimezone, userTimeFormat);
     const isDefaultAgent = sessionAgentId === defaultAgentId;
     const promptMode =
-      isSubagentSessionKey(params.sessionKey) || isCronSessionKey(params.sessionKey)
+      (params.promptMode as "raw" | undefined) ??
+      (isSubagentSessionKey(params.sessionKey) || isCronSessionKey(params.sessionKey)
         ? "minimal"
-        : "full";
+        : "full");
     const docsPath = await resolveOpenClawDocsPath({
       workspaceDir: effectiveWorkspace,
       argv1: process.argv[1],
diff --git a/src/agents/pi-embedded-runner/run.ts b/src/agents/pi-embedded-runner/run.ts
index 1b9963fac2..a88ecfe91a 100644
--- a/src/agents/pi-embedded-runner/run.ts
+++ b/src/agents/pi-embedded-runner/run.ts
@@ -548,6 +548,7 @@ export async function runEmbeddedPiAgent(
             onToolResult: params.onToolResult,
             onAgentEvent: params.onAgentEvent,
             extraSystemPrompt: params.extraSystemPrompt,
+            promptMode: params.promptMode,
             inputProvenance: params.inputProvenance,
             streamParams: params.streamParams,
             ownerNumbers: params.ownerNumbers,
diff --git a/src/agents/pi-embedded-runner/run/attempt.ts b/src/agents/pi-embedded-runner/run/attempt.ts
index e88b7430f2..1989220c3b 100644
--- a/src/agents/pi-embedded-runner/run/attempt.ts
+++ b/src/agents/pi-embedded-runner/run/attempt.ts
@@ -636,7 +636,8 @@ export async function runEmbeddedAttempt(
       },
     });
     const isDefaultAgent = sessionAgentId === defaultAgentId;
-    const promptMode = resolvePromptModeForSession(params.sessionKey);
+    const promptMode =
+      (params.promptMode as "raw" | undefined) ?? resolvePromptModeForSession(params.sessionKey);
 
     // When toolsAllow is set, use minimal prompt and strip skills catalog
     const effectivePromptMode = params.toolsAllow?.length ? ("minimal" as const) : promptMode;
@@ -1430,6 +1431,7 @@ export async function runEmbeddedAttempt(
           messageProvider: params.messageProvider ?? undefined,
           trigger: params.trigger,
           channelId: params.messageChannel ?? params.messageProvider ?? undefined,
+          promptMode,
         };
         const hookResult = await resolvePromptBuildHookResult({
           prompt: params.prompt,
diff --git a/src/agents/pi-embedded-runner/run/params.ts b/src/agents/pi-embedded-runner/run/params.ts
index d8e9d8bdfd..82492112a2 100644
--- a/src/agents/pi-embedded-runner/run/params.ts
+++ b/src/agents/pi-embedded-runner/run/params.ts
@@ -142,4 +142,6 @@ export type RunEmbeddedPiAgentParams = {
    * exit promptly after emitting the final JSON result.
    */
   cleanupBundleMcpOnRunEnd?: boolean;
+  /** Override the automatic promptMode resolution (e.g. "raw" for custom persona agents). */
+  promptMode?: string;
 };
diff --git a/src/agents/system-prompt.ts b/src/agents/system-prompt.ts
index 5a0ef6f871..c11b9b5b0b 100644
--- a/src/agents/system-prompt.ts
+++ b/src/agents/system-prompt.ts
@@ -15,7 +15,7 @@ import { sanitizeForPromptLiteral } from "./sanitize-for-prompt.js";
  * - "minimal": Reduced sections (Tooling, Workspace, Runtime) - used for subagents
  * - "none": Just basic identity line, no sections
  */
-export type PromptMode = "full" | "minimal" | "none";
+export type PromptMode = "full" | "minimal" | "none" | "raw";
 type OwnerIdDisplay = "raw" | "hash";
 
 function buildSkillsSection(params: { skillsPrompt?: string; readToolName: string }) {
@@ -413,6 +413,14 @@ export function buildAgentSystemPrompt(params: {
   });
   const workspaceNotes = (params.workspaceNotes ?? []).map((note) => note.trim()).filter(Boolean);
 
+  // For "raw" mode, return only the caller-supplied extraSystemPrompt with no
+  // hardcoded identity, safety, runtime, or tooling sections. This lets the
+  // caller fully control the agent persona (e.g. customer-service agents that
+  // must not reveal they are AI).
+  if (promptMode === "raw") {
+    return extraSystemPrompt ?? "";
+  }
+
   // For "none" mode, return just the basic identity line
   if (promptMode === "none") {
     return "You are a personal assistant running inside OpenClaw.";
diff --git a/src/gateway/protocol/schema/agent.ts b/src/gateway/protocol/schema/agent.ts
index b9c844b135..52a650f85c 100644
--- a/src/gateway/protocol/schema/agent.ts
+++ b/src/gateway/protocol/schema/agent.ts
@@ -96,6 +96,7 @@ export const AgentParamsSchema = Type.Object(
     bestEffortDeliver: Type.Optional(Type.Boolean()),
     lane: Type.Optional(Type.String()),
     extraSystemPrompt: Type.Optional(Type.String()),
+    promptMode: Type.Optional(Type.String()),
     internalEvents: Type.Optional(Type.Array(AgentInternalEventSchema)),
     inputProvenance: Type.Optional(InputProvenanceSchema),
     idempotencyKey: NonEmptyString,
diff --git a/src/gateway/server-methods/agent.ts b/src/gateway/server-methods/agent.ts
index 4f244f1c7c..6865ef03ca 100644
--- a/src/gateway/server-methods/agent.ts
+++ b/src/gateway/server-methods/agent.ts
@@ -301,6 +301,7 @@ export const agentHandlers: GatewayRequestHandlers = {
       groupSpace?: string;
       lane?: string;
       extraSystemPrompt?: string;
+      promptMode?: string;
       internalEvents?: AgentInternalEvent[];
       idempotencyKey: string;
       timeout?: number;
@@ -836,6 +837,7 @@ export const agentHandlers: GatewayRequestHandlers = {
         runId,
         lane: request.lane,
         extraSystemPrompt: request.extraSystemPrompt,
+        promptMode: request.promptMode,
         internalEvents: request.internalEvents,
         inputProvenance,
         // Internal-only: allow workspace override for spawned subagent runs.
-- 
2.50.1 (Apple Git-155)

