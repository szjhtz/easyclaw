From e7a75dfb510103f20f55c1634c7fb2cb9cd101d3 Mon Sep 17 00:00:00 2001
From: RivonClaw CI <ci@rivonclaw.com>
Date: Thu, 2 Apr 2026 01:09:28 -0700
Subject: [PATCH 1/4] vendor(openclaw): add browser lifecycle hooks for plugin
 integration

Why:
- RivonClaw needs to track browser session lifecycle (start/stop) to
  persist session state (cookies) across restarts
- RivonClaw needs before/after action hooks for future extensibility
  (auth injection, captcha solving, page preparation)
- Existing before_tool_call/after_tool_call hooks lack browser-specific
  context and proper timing semantics

Removal:
- When OpenClaw adds native browser lifecycle hooks upstream
- Track: propose as upstream PR after proving the design in RivonClaw

Tests:
- extensions/browser-profiles-tools/src/browser-lifecycle-hooks.test.ts
---
 extensions/browser/src/browser-tool.ts | 622 ++++++++++++++-----------
 extensions/browser/src/core-api.ts     |   1 +
 src/plugin-sdk/browser-support.ts      |   1 +
 src/plugins/hooks.ts                   |  74 +++
 src/plugins/types.ts                   |  70 ++-
 5 files changed, 489 insertions(+), 279 deletions(-)

diff --git a/extensions/browser/src/browser-tool.ts b/extensions/browser/src/browser-tool.ts
index cf0792fe31..a045f632d8 100644
--- a/extensions/browser/src/browser-tool.ts
+++ b/extensions/browser/src/browser-tool.ts
@@ -11,6 +11,7 @@ import {
   type NodeListNode,
   DEFAULT_UPLOAD_DIR,
   applyBrowserProxyPaths,
+  getGlobalHookRunner,
   browserAct,
   browserArmDialog,
   browserArmFileChooser,
@@ -453,302 +454,367 @@ export function createBrowserTool(opts?: {
           }
         : null;
 
-      switch (action) {
-        case "status":
-          if (proxyRequest) {
-            return jsonResult(
-              await proxyRequest({
-                method: "GET",
-                path: "/",
-                profile,
-              }),
-            );
-          }
-          return jsonResult(await browserToolDeps.browserStatus(baseUrl, { profile }));
-        case "start":
-          if (proxyRequest) {
-            await proxyRequest({
-              method: "POST",
-              path: "/start",
-              profile,
-            });
-            return jsonResult(
-              await proxyRequest({
-                method: "GET",
-                path: "/",
-                profile,
-              }),
-            );
-          }
-          await browserToolDeps.browserStart(baseUrl, { profile });
-          return jsonResult(await browserToolDeps.browserStatus(baseUrl, { profile }));
-        case "stop":
-          if (proxyRequest) {
-            await proxyRequest({
-              method: "POST",
-              path: "/stop",
-              profile,
-            });
-            return jsonResult(
-              await proxyRequest({
-                method: "GET",
-                path: "/",
-                profile,
-              }),
-            );
-          }
-          await browserToolDeps.browserStop(baseUrl, { profile });
-          return jsonResult(await browserToolDeps.browserStatus(baseUrl, { profile }));
-        case "profiles":
-          if (proxyRequest) {
-            const result = await proxyRequest({
-              method: "GET",
-              path: "/profiles",
-            });
-            return jsonResult(result);
-          }
-          return jsonResult({ profiles: await browserToolDeps.browserProfiles(baseUrl) });
-        case "tabs":
-          return await executeTabsAction({ baseUrl, profile, proxyRequest });
-        case "open": {
-          const targetUrl = readTargetUrlParam(params);
-          if (proxyRequest) {
-            const result = await proxyRequest({
-              method: "POST",
-              path: "/tabs/open",
-              profile,
-              body: { url: targetUrl },
-            });
-            return jsonResult(result);
-          }
-          const opened = await browserToolDeps.browserOpenTab(baseUrl, targetUrl, { profile });
-          browserToolDeps.trackSessionBrowserTab({
-            sessionKey: opts?.agentSessionKey,
-            targetId: opened.targetId,
-            baseUrl,
-            profile,
-          });
-          return jsonResult(opened);
+      // --- Browser lifecycle hooks (RivonClaw patch) ---
+      const hookRunner = getGlobalHookRunner();
+      const browserHookCtx = { sessionKey: opts?.agentSessionKey, profile };
+
+      // before_browser_action: let plugins inspect/modify/block non-lifecycle actions
+      if (
+        hookRunner?.hasHooks("before_browser_action") &&
+        action !== "start" &&
+        action !== "stop" &&
+        action !== "status" &&
+        action !== "profiles"
+      ) {
+        const beforeResult = await hookRunner.runBeforeBrowserAction(
+          { action, params, profile },
+          browserHookCtx,
+        );
+        if (beforeResult?.block) {
+          throw new Error(beforeResult.blockReason ?? "Browser action blocked by plugin");
         }
-        case "focus": {
-          const targetId = readStringParam(params, "targetId", {
-            required: true,
-          });
-          if (proxyRequest) {
-            const result = await proxyRequest({
-              method: "POST",
-              path: "/tabs/focus",
-              profile,
-              body: { targetId },
-            });
-            return jsonResult(result);
-          }
-          await browserToolDeps.browserFocusTab(baseUrl, targetId, { profile });
-          return jsonResult({ ok: true });
+        if (beforeResult?.params) {
+          Object.assign(params, beforeResult.params);
         }
-        case "close": {
-          const targetId = readStringParam(params, "targetId");
-          if (proxyRequest) {
-            const result = targetId
-              ? await proxyRequest({
-                  method: "DELETE",
-                  path: `/tabs/${encodeURIComponent(targetId)}`,
+      }
+
+      // Wrap the action switch in try/finally so after_browser_action fires uniformly
+      const actionStartTime = Date.now();
+      let actionError: string | undefined;
+      try {
+        const actionResult = await (async () => {
+          switch (action) {
+            case "status":
+              if (proxyRequest) {
+                return jsonResult(
+                  await proxyRequest({
+                    method: "GET",
+                    path: "/",
+                    profile,
+                  }),
+                );
+              }
+              return jsonResult(await browserToolDeps.browserStatus(baseUrl, { profile }));
+            case "start":
+              if (proxyRequest) {
+                await proxyRequest({
+                  method: "POST",
+                  path: "/start",
                   profile,
-                })
-              : await proxyRequest({
+                });
+                return jsonResult(
+                  await proxyRequest({
+                    method: "GET",
+                    path: "/",
+                    profile,
+                  }),
+                );
+              }
+              await browserToolDeps.browserStart(baseUrl, { profile });
+              return jsonResult(await browserToolDeps.browserStatus(baseUrl, { profile }));
+            case "stop":
+              if (proxyRequest) {
+                await proxyRequest({
                   method: "POST",
-                  path: "/act",
+                  path: "/stop",
                   profile,
-                  body: { kind: "close" },
                 });
-            return jsonResult(result);
-          }
-          if (targetId) {
-            await browserToolDeps.browserCloseTab(baseUrl, targetId, { profile });
-            browserToolDeps.untrackSessionBrowserTab({
-              sessionKey: opts?.agentSessionKey,
-              targetId,
-              baseUrl,
-              profile,
-            });
-          } else {
-            await browserToolDeps.browserAct(baseUrl, { kind: "close" }, { profile });
-          }
-          return jsonResult({ ok: true });
-        }
-        case "snapshot":
-          return await executeSnapshotAction({
-            input: params,
-            baseUrl,
-            profile,
-            proxyRequest,
-          });
-        case "screenshot": {
-          const targetId = readStringParam(params, "targetId");
-          const fullPage = Boolean(params.fullPage);
-          const ref = readStringParam(params, "ref");
-          const element = readStringParam(params, "element");
-          const type = params.type === "jpeg" ? "jpeg" : "png";
-          const result = proxyRequest
-            ? ((await proxyRequest({
-                method: "POST",
-                path: "/screenshot",
+                return jsonResult(
+                  await proxyRequest({
+                    method: "GET",
+                    path: "/",
+                    profile,
+                  }),
+                );
+              }
+              await browserToolDeps.browserStop(baseUrl, { profile });
+              return jsonResult(await browserToolDeps.browserStatus(baseUrl, { profile }));
+            case "profiles":
+              if (proxyRequest) {
+                const result = await proxyRequest({
+                  method: "GET",
+                  path: "/profiles",
+                });
+                return jsonResult(result);
+              }
+              return jsonResult({ profiles: await browserToolDeps.browserProfiles(baseUrl) });
+            case "tabs":
+              return await executeTabsAction({ baseUrl, profile, proxyRequest });
+            case "open": {
+              const targetUrl = readTargetUrlParam(params);
+              if (proxyRequest) {
+                const result = await proxyRequest({
+                  method: "POST",
+                  path: "/tabs/open",
+                  profile,
+                  body: { url: targetUrl },
+                });
+                return jsonResult(result);
+              }
+              const opened = await browserToolDeps.browserOpenTab(baseUrl, targetUrl, { profile });
+              browserToolDeps.trackSessionBrowserTab({
+                sessionKey: opts?.agentSessionKey,
+                targetId: opened.targetId,
+                baseUrl,
                 profile,
-                body: {
+              });
+              return jsonResult(opened);
+            }
+            case "focus": {
+              const targetId = readStringParam(params, "targetId", {
+                required: true,
+              });
+              if (proxyRequest) {
+                const result = await proxyRequest({
+                  method: "POST",
+                  path: "/tabs/focus",
+                  profile,
+                  body: { targetId },
+                });
+                return jsonResult(result);
+              }
+              await browserToolDeps.browserFocusTab(baseUrl, targetId, { profile });
+              return jsonResult({ ok: true });
+            }
+            case "close": {
+              const targetId = readStringParam(params, "targetId");
+              if (proxyRequest) {
+                const result = targetId
+                  ? await proxyRequest({
+                      method: "DELETE",
+                      path: `/tabs/${encodeURIComponent(targetId)}`,
+                      profile,
+                    })
+                  : await proxyRequest({
+                      method: "POST",
+                      path: "/act",
+                      profile,
+                      body: { kind: "close" },
+                    });
+                return jsonResult(result);
+              }
+              if (targetId) {
+                await browserToolDeps.browserCloseTab(baseUrl, targetId, { profile });
+                browserToolDeps.untrackSessionBrowserTab({
+                  sessionKey: opts?.agentSessionKey,
                   targetId,
-                  fullPage,
+                  baseUrl,
+                  profile,
+                });
+              } else {
+                await browserToolDeps.browserAct(baseUrl, { kind: "close" }, { profile });
+              }
+              return jsonResult({ ok: true });
+            }
+            case "snapshot":
+              return await executeSnapshotAction({
+                input: params,
+                baseUrl,
+                profile,
+                proxyRequest,
+              });
+            case "screenshot": {
+              const targetId = readStringParam(params, "targetId");
+              const fullPage = Boolean(params.fullPage);
+              const ref = readStringParam(params, "ref");
+              const element = readStringParam(params, "element");
+              const type = params.type === "jpeg" ? "jpeg" : "png";
+              const result = proxyRequest
+                ? ((await proxyRequest({
+                    method: "POST",
+                    path: "/screenshot",
+                    profile,
+                    body: {
+                      targetId,
+                      fullPage,
+                      ref,
+                      element,
+                      type,
+                    },
+                  })) as Awaited<ReturnType<typeof browserScreenshotAction>>)
+                : await browserToolDeps.browserScreenshotAction(baseUrl, {
+                    targetId,
+                    fullPage,
+                    ref,
+                    element,
+                    type,
+                    profile,
+                  });
+              return await browserToolDeps.imageResultFromFile({
+                label: "browser:screenshot",
+                path: result.path,
+                details: result,
+              });
+            }
+            case "navigate": {
+              const targetUrl = readTargetUrlParam(params);
+              const targetId = readStringParam(params, "targetId");
+              if (proxyRequest) {
+                const result = await proxyRequest({
+                  method: "POST",
+                  path: "/navigate",
+                  profile,
+                  body: {
+                    url: targetUrl,
+                    targetId,
+                  },
+                });
+                return jsonResult(result);
+              }
+              return jsonResult(
+                await browserToolDeps.browserNavigate(baseUrl, {
+                  url: targetUrl,
+                  targetId,
+                  profile,
+                }),
+              );
+            }
+            case "console":
+              return await executeConsoleAction({
+                input: params,
+                baseUrl,
+                profile,
+                proxyRequest,
+              });
+            case "pdf": {
+              const targetId =
+                typeof params.targetId === "string" ? params.targetId.trim() : undefined;
+              const result = proxyRequest
+                ? ((await proxyRequest({
+                    method: "POST",
+                    path: "/pdf",
+                    profile,
+                    body: { targetId },
+                  })) as Awaited<ReturnType<typeof browserPdfSave>>)
+                : await browserToolDeps.browserPdfSave(baseUrl, { targetId, profile });
+              return {
+                content: [{ type: "text" as const, text: `FILE:${result.path}` }],
+                details: result,
+              };
+            }
+            case "upload": {
+              const paths = Array.isArray(params.paths) ? params.paths.map((p) => String(p)) : [];
+              if (paths.length === 0) {
+                throw new Error("paths required");
+              }
+              const uploadPathsResult = await resolveExistingPathsWithinRoot({
+                rootDir: DEFAULT_UPLOAD_DIR,
+                requestedPaths: paths,
+                scopeLabel: `uploads directory (${DEFAULT_UPLOAD_DIR})`,
+              });
+              if (!uploadPathsResult.ok) {
+                throw new Error(uploadPathsResult.error);
+              }
+              const normalizedPaths = uploadPathsResult.paths;
+              const ref = readStringParam(params, "ref");
+              const inputRef = readStringParam(params, "inputRef");
+              const element = readStringParam(params, "element");
+              const { targetId, timeoutMs } = readOptionalTargetAndTimeout(params);
+              if (proxyRequest) {
+                const result = await proxyRequest({
+                  method: "POST",
+                  path: "/hooks/file-chooser",
+                  profile,
+                  body: {
+                    paths: normalizedPaths,
+                    ref,
+                    inputRef,
+                    element,
+                    targetId,
+                    timeoutMs,
+                  },
+                });
+                return jsonResult(result);
+              }
+              return jsonResult(
+                await browserToolDeps.browserArmFileChooser(baseUrl, {
+                  paths: normalizedPaths,
                   ref,
+                  inputRef,
                   element,
-                  type,
-                },
-              })) as Awaited<ReturnType<typeof browserScreenshotAction>>)
-            : await browserToolDeps.browserScreenshotAction(baseUrl, {
-                targetId,
-                fullPage,
-                ref,
-                element,
-                type,
+                  targetId,
+                  timeoutMs,
+                  profile,
+                }),
+              );
+            }
+            case "dialog": {
+              const accept = Boolean(params.accept);
+              const promptText =
+                typeof params.promptText === "string" ? params.promptText : undefined;
+              const { targetId, timeoutMs } = readOptionalTargetAndTimeout(params);
+              if (proxyRequest) {
+                const result = await proxyRequest({
+                  method: "POST",
+                  path: "/hooks/dialog",
+                  profile,
+                  body: {
+                    accept,
+                    promptText,
+                    targetId,
+                    timeoutMs,
+                  },
+                });
+                return jsonResult(result);
+              }
+              return jsonResult(
+                await browserToolDeps.browserArmDialog(baseUrl, {
+                  accept,
+                  promptText,
+                  targetId,
+                  timeoutMs,
+                  profile,
+                }),
+              );
+            }
+            case "act": {
+              const request = readActRequestParam(params);
+              if (!request) {
+                throw new Error("request required");
+              }
+              return await executeActAction({
+                request,
+                baseUrl,
                 profile,
+                proxyRequest,
               });
-          return await browserToolDeps.imageResultFromFile({
-            label: "browser:screenshot",
-            path: result.path,
-            details: result,
-          });
-        }
-        case "navigate": {
-          const targetUrl = readTargetUrlParam(params);
-          const targetId = readStringParam(params, "targetId");
-          if (proxyRequest) {
-            const result = await proxyRequest({
-              method: "POST",
-              path: "/navigate",
-              profile,
-              body: {
-                url: targetUrl,
-                targetId,
-              },
-            });
-            return jsonResult(result);
+            }
+            default:
+              throw new Error(`Unknown action: ${action}`);
           }
-          return jsonResult(
-            await browserToolDeps.browserNavigate(baseUrl, {
-              url: targetUrl,
-              targetId,
-              profile,
-            }),
-          );
+        })();
+
+        // browser_session_start: fires after successful browser start
+        if (action === "start" && hookRunner?.hasHooks("browser_session_start")) {
+          void hookRunner.runBrowserSessionStart({ profile, action: "start" }, browserHookCtx);
         }
-        case "console":
-          return await executeConsoleAction({
-            input: params,
-            baseUrl,
-            profile,
-            proxyRequest,
-          });
-        case "pdf": {
-          const targetId = typeof params.targetId === "string" ? params.targetId.trim() : undefined;
-          const result = proxyRequest
-            ? ((await proxyRequest({
-                method: "POST",
-                path: "/pdf",
-                profile,
-                body: { targetId },
-              })) as Awaited<ReturnType<typeof browserPdfSave>>)
-            : await browserToolDeps.browserPdfSave(baseUrl, { targetId, profile });
-          return {
-            content: [{ type: "text" as const, text: `FILE:${result.path}` }],
-            details: result,
-          };
+        // browser_session_end: fires after successful browser stop
+        if (action === "stop" && hookRunner?.hasHooks("browser_session_end")) {
+          void hookRunner.runBrowserSessionEnd({ profile, action: "stop" }, browserHookCtx);
         }
-        case "upload": {
-          const paths = Array.isArray(params.paths) ? params.paths.map((p) => String(p)) : [];
-          if (paths.length === 0) {
-            throw new Error("paths required");
-          }
-          const uploadPathsResult = await resolveExistingPathsWithinRoot({
-            rootDir: DEFAULT_UPLOAD_DIR,
-            requestedPaths: paths,
-            scopeLabel: `uploads directory (${DEFAULT_UPLOAD_DIR})`,
-          });
-          if (!uploadPathsResult.ok) {
-            throw new Error(uploadPathsResult.error);
-          }
-          const normalizedPaths = uploadPathsResult.paths;
-          const ref = readStringParam(params, "ref");
-          const inputRef = readStringParam(params, "inputRef");
-          const element = readStringParam(params, "element");
-          const { targetId, timeoutMs } = readOptionalTargetAndTimeout(params);
-          if (proxyRequest) {
-            const result = await proxyRequest({
-              method: "POST",
-              path: "/hooks/file-chooser",
-              profile,
-              body: {
-                paths: normalizedPaths,
-                ref,
-                inputRef,
-                element,
-                targetId,
-                timeoutMs,
-              },
-            });
-            return jsonResult(result);
-          }
-          return jsonResult(
-            await browserToolDeps.browserArmFileChooser(baseUrl, {
-              paths: normalizedPaths,
-              ref,
-              inputRef,
-              element,
-              targetId,
-              timeoutMs,
-              profile,
-            }),
-          );
-        }
-        case "dialog": {
-          const accept = Boolean(params.accept);
-          const promptText = typeof params.promptText === "string" ? params.promptText : undefined;
-          const { targetId, timeoutMs } = readOptionalTargetAndTimeout(params);
-          if (proxyRequest) {
-            const result = await proxyRequest({
-              method: "POST",
-              path: "/hooks/dialog",
-              profile,
-              body: {
-                accept,
-                promptText,
-                targetId,
-                timeoutMs,
-              },
-            });
-            return jsonResult(result);
-          }
-          return jsonResult(
-            await browserToolDeps.browserArmDialog(baseUrl, {
-              accept,
-              promptText,
-              targetId,
-              timeoutMs,
+
+        return actionResult;
+      } catch (err) {
+        actionError = String(err);
+        throw err;
+      } finally {
+        // after_browser_action: fires for all non-metadata actions
+        if (
+          hookRunner?.hasHooks("after_browser_action") &&
+          action !== "status" &&
+          action !== "profiles"
+        ) {
+          void hookRunner.runAfterBrowserAction(
+            {
+              action,
+              params,
               profile,
-            }),
+              error: actionError,
+              durationMs: Date.now() - actionStartTime,
+            },
+            browserHookCtx,
           );
         }
-        case "act": {
-          const request = readActRequestParam(params);
-          if (!request) {
-            throw new Error("request required");
-          }
-          return await executeActAction({
-            request,
-            baseUrl,
-            profile,
-            proxyRequest,
-          });
-        }
-        default:
-          throw new Error(`Unknown action: ${action}`);
       }
     },
   };
diff --git a/extensions/browser/src/core-api.ts b/extensions/browser/src/core-api.ts
index a7e5f4595a..101a6289d4 100644
--- a/extensions/browser/src/core-api.ts
+++ b/extensions/browser/src/core-api.ts
@@ -65,6 +65,7 @@ export type {
 export {
   callGatewayTool,
   createSubsystemLogger,
+  getGlobalHookRunner,
   danger,
   defaultRuntime,
   detectMime,
diff --git a/src/plugin-sdk/browser-support.ts b/src/plugin-sdk/browser-support.ts
index 2a053f8f09..416b1d97a0 100644
--- a/src/plugin-sdk/browser-support.ts
+++ b/src/plugin-sdk/browser-support.ts
@@ -22,6 +22,7 @@ export {
   resizeToJpeg,
 } from "../media/image-ops.js";
 export { ensureMediaDir, saveMediaBuffer } from "../media/store.js";
+export { getGlobalHookRunner } from "../plugins/hook-runner-global.js";
 export { normalizePluginsConfig, resolveEffectiveEnableState } from "../plugins/config-state.js";
 export {
   startLazyPluginServiceModule,
diff --git a/src/plugins/hooks.ts b/src/plugins/hooks.ts
index 0583b22ce5..667ca8bdcb 100644
--- a/src/plugins/hooks.ts
+++ b/src/plugins/hooks.ts
@@ -56,6 +56,12 @@ import type {
   PluginHookToolResultPersistResult,
   PluginHookBeforeMessageWriteEvent,
   PluginHookBeforeMessageWriteResult,
+  PluginHookBrowserContext,
+  PluginHookBrowserSessionStartEvent,
+  PluginHookBrowserSessionEndEvent,
+  PluginHookBeforeBrowserActionEvent,
+  PluginHookBeforeBrowserActionResult,
+  PluginHookAfterBrowserActionEvent,
 } from "./types.js";
 
 // Re-export types for consumers
@@ -227,6 +233,15 @@ export function createHookRunner(registry: PluginRegistry, options: HookRunnerOp
     };
   };
 
+  const mergeBeforeBrowserAction = (
+    acc: PluginHookBeforeBrowserActionResult | undefined,
+    next: PluginHookBeforeBrowserActionResult,
+  ): PluginHookBeforeBrowserActionResult => ({
+    params: next.params ?? acc?.params,
+    block: acc?.block || next.block,
+    blockReason: acc?.blockReason ?? next.blockReason,
+  });
+
   const mergeSubagentDeliveryTargetResult = (
     acc: PluginHookSubagentDeliveryTargetResult | undefined,
     next: PluginHookSubagentDeliveryTargetResult,
@@ -978,6 +993,60 @@ export function createHookRunner(registry: PluginRegistry, options: HookRunnerOp
     return runVoidHook("gateway_stop", event, ctx);
   }
 
+  // =========================================================================
+  // Browser Hooks
+  // =========================================================================
+
+  /**
+   * Run browser_session_start hook.
+   * Runs in parallel (fire-and-forget).
+   */
+  async function runBrowserSessionStart(
+    event: PluginHookBrowserSessionStartEvent,
+    ctx: PluginHookBrowserContext,
+  ): Promise<void> {
+    await runVoidHook("browser_session_start", event, ctx);
+  }
+
+  /**
+   * Run browser_session_end hook.
+   * Runs in parallel (fire-and-forget).
+   */
+  async function runBrowserSessionEnd(
+    event: PluginHookBrowserSessionEndEvent,
+    ctx: PluginHookBrowserContext,
+  ): Promise<void> {
+    await runVoidHook("browser_session_end", event, ctx);
+  }
+
+  /**
+   * Run before_browser_action hook.
+   * Allows plugins to modify or block browser actions.
+   * Runs sequentially.
+   */
+  async function runBeforeBrowserAction(
+    event: PluginHookBeforeBrowserActionEvent,
+    ctx: PluginHookBrowserContext,
+  ): Promise<PluginHookBeforeBrowserActionResult | undefined> {
+    return runModifyingHook<"before_browser_action", PluginHookBeforeBrowserActionResult>(
+      "before_browser_action",
+      event,
+      ctx,
+      { mergeResults: mergeBeforeBrowserAction },
+    );
+  }
+
+  /**
+   * Run after_browser_action hook.
+   * Runs in parallel (fire-and-forget).
+   */
+  async function runAfterBrowserAction(
+    event: PluginHookAfterBrowserActionEvent,
+    ctx: PluginHookBrowserContext,
+  ): Promise<void> {
+    await runVoidHook("after_browser_action", event, ctx);
+  }
+
   // =========================================================================
   // Utility
   // =========================================================================
@@ -1031,6 +1100,11 @@ export function createHookRunner(registry: PluginRegistry, options: HookRunnerOp
     // Gateway hooks
     runGatewayStart,
     runGatewayStop,
+    // Browser hooks
+    runBrowserSessionStart,
+    runBrowserSessionEnd,
+    runBeforeBrowserAction,
+    runAfterBrowserAction,
     // Utility
     hasHooks,
     getHookCount,
diff --git a/src/plugins/types.ts b/src/plugins/types.ts
index 02bb7f985c..7a731654d1 100644
--- a/src/plugins/types.ts
+++ b/src/plugins/types.ts
@@ -1819,7 +1819,11 @@ export type PluginHookName =
   | "subagent_ended"
   | "gateway_start"
   | "gateway_stop"
-  | "before_dispatch";
+  | "before_dispatch"
+  | "browser_session_start"
+  | "browser_session_end"
+  | "before_browser_action"
+  | "after_browser_action";
 
 export const PLUGIN_HOOK_NAMES = [
   "before_model_resolve",
@@ -1848,6 +1852,10 @@ export const PLUGIN_HOOK_NAMES = [
   "gateway_start",
   "gateway_stop",
   "before_dispatch",
+  "browser_session_start",
+  "browser_session_end",
+  "before_browser_action",
+  "after_browser_action",
 ] as const satisfies readonly PluginHookName[];
 
 type MissingPluginHookNames = Exclude<PluginHookName, (typeof PLUGIN_HOOK_NAMES)[number]>;
@@ -2353,6 +2361,47 @@ export type PluginHookGatewayStopEvent = {
   reason?: string;
 };
 
+// Browser context (shared across browser hooks)
+export type PluginHookBrowserContext = {
+  sessionKey?: string;
+  profile?: string;
+};
+
+// browser_session_start hook
+export type PluginHookBrowserSessionStartEvent = {
+  profile?: string;
+  action: "start";
+};
+
+// browser_session_end hook
+export type PluginHookBrowserSessionEndEvent = {
+  profile?: string;
+  action: "stop";
+};
+
+// before_browser_action hook
+export type PluginHookBeforeBrowserActionEvent = {
+  action: string;
+  params: Record<string, unknown>;
+  profile?: string;
+};
+
+export type PluginHookBeforeBrowserActionResult = {
+  params?: Record<string, unknown>;
+  block?: boolean;
+  blockReason?: string;
+};
+
+// after_browser_action hook
+export type PluginHookAfterBrowserActionEvent = {
+  action: string;
+  params: Record<string, unknown>;
+  profile?: string;
+  result?: unknown;
+  error?: string;
+  durationMs?: number;
+};
+
 export type PluginInstallRequestKind =
   | "skill-install"
   | "plugin-dir"
@@ -2466,6 +2515,25 @@ export type PluginHookHandlerMap = {
     event: PluginHookGatewayStopEvent,
     ctx: PluginHookGatewayContext,
   ) => Promise<void> | void;
+  browser_session_start: (
+    event: PluginHookBrowserSessionStartEvent,
+    ctx: PluginHookBrowserContext,
+  ) => Promise<void> | void;
+  browser_session_end: (
+    event: PluginHookBrowserSessionEndEvent,
+    ctx: PluginHookBrowserContext,
+  ) => Promise<void> | void;
+  before_browser_action: (
+    event: PluginHookBeforeBrowserActionEvent,
+    ctx: PluginHookBrowserContext,
+  ) =>
+    | Promise<PluginHookBeforeBrowserActionResult | void>
+    | PluginHookBeforeBrowserActionResult
+    | void;
+  after_browser_action: (
+    event: PluginHookAfterBrowserActionEvent,
+    ctx: PluginHookBrowserContext,
+  ) => Promise<void> | void;
 };
 
 export type PluginHookRegistration<K extends PluginHookName = PluginHookName> = {
-- 
2.50.1 (Apple Git-155)

